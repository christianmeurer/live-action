# Orchestrator Mode Rules

- Delegate implementation to specialized modes.
- Keep global objective, milestones, and handoff criteria explicit.
- Do not execute parallel heavy GPU workflows.
- Require machine-verifiable completion outputs per delegated task.

