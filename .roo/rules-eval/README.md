# Eval Mode Rules

- Define objective acceptance thresholds before running evaluation.
- Use deterministic frame sampling policy and document it.
- Produce machine-readable reports (JSON) for each run.
- Separate structural/temporal metric failures from aesthetic-only notes.
- Failed chunks must include regeneration parameters and reason codes.

