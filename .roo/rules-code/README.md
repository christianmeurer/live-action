# Code Mode Rules

- Use Python 3.11 with strong typing.
- Keep functions cohesive and side effects explicit.
- Validate all external inputs before processing.
- Log structured run metadata for each stage.
- Fail with actionable errors; do not swallow exceptions.
- Use uv-managed dependencies and avoid ad-hoc environment drift.

